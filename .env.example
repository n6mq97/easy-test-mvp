# PostgreSQL connection settings
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=testdb

# Full database connection URL for the backend
DATABASE_URL=postgresql://user:password@localhost/testdb
